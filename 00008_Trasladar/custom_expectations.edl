expectation "Para resolver este ejercicio recordá que contás con `agregar` y `remover` (versiones simplificadas de `push` y `pop` respectivamente).":
  uses `agregar` || uses `push` && uses `remover` || uses `pop`;